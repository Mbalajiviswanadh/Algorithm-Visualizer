export type SortingAlforithmType =
  | "bubble"
  | "insertion"
  | "selection"
  | "merge"
  | "quick";

export type SelectoptionsType = {
  value: string;
  label: string;
};

export type AnimationArrayType = [number[], boolean][];
